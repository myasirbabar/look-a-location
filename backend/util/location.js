async function getAddressCoords(address) {
  	// Random Assigning Since Google Api is not available right now.
	return {
		lat: 31.4804833,
		lng: 74.2630103,
	};
}

module.exports = getAddressCoords;